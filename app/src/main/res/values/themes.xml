<resources>
    <!-- Base window theme. Kept minimal on purpose: the Compose UI applies its
         own Material 3 theme, and enableEdgeToEdge() manages the system bars and
         cutout handling across API levels. Setting android:statusBarColor /
         windowLayoutInDisplayCutoutMode here is deprecated on Android 15+, so
         those are intentionally omitted. -->
    <style name="Theme.SiteWatch" parent="android:Theme.Material.Light.NoActionBar" />
</resources>
